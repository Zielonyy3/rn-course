import {StyleSheet, Text} from 'react-native';

function FavoritesScreen({navigation}) {
    return (
        <Text>Favortie screen</Text>
    );
}

export default FavoritesScreen;


const styles = StyleSheet.create({});
